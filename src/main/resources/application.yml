spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/examdb
    username: ksu
    password:

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true